# Pantheon MCP runtime dependencies — EXACT pins (issue #21)
# Catalog copy of requirements-mcp.txt (kept in sync — the installer uses
# requirements-mcp.txt via scripts/install/venv.mjs).
mcp==1.29.0
fastmcp==3.4.6
pyyaml==6.0.3
sqlite-vec==0.1.9
fastembed==0.8.0
