# Pantheon MCP runtime dependencies — EXACT pins (issue #21)
# Pinned to the versions validated in the sandbox venv (2026-08-05).
# Update deliberately via: pip install -U <pkg> && pip freeze | grep -iE '^(mcp|fastmcp|pyyaml)'
# Memory search is FTS5 (SQLite stdlib): sqlite-vec and fastembed were removed
# with the vector pipeline — the server no longer imports either.
mcp==1.29.0
fastmcp==4.0.5
pyyaml==6.0.3
